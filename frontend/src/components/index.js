// Importing components from their respective files
import Card from './Card'; // Importing the Card component
import FormField from './FormField'; // Importing the FormField component
import Loader from './Loader'; // Importing the Loader component

// Exporting the components to make them available for use in other parts of the app
export {
  Card, // Exporting Card component
  FormField, // Exporting FormField component
  Loader, // Exporting Loader component
};
